services:
  mongo:
    image: mongo:8
    container_name: futures-trader
    restart: always
    ports:
      - "10001:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: 123456
    volumes:
      - futures-trader:/data/db

volumes:
  futures-trader:
